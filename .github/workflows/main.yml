name: Main
on:
  push:
    branches:
      - main
    tags:
      - baloney*
  issue_comment:
    types:
      - created

jobs:
  random-job:
    runs-on: ubuntu-latest
    name: main job
    steps:
      - name: echo
        run: echo this is the way
  run-action:
    runs-on: ubuntu-latest
    uses: ./.github/actions/sonar-report
    with:
      who-to-greet: "stu"
